import { JoinedTodayPipe } from './joined-today.pipe';

describe('JoinedTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinedTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
